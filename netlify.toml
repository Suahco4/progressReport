[build]
  command = ""
  publish = "."

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

# Mimic express.static: If a file isn't found in root, try looking in /public/
[[redirects]]
  from = "/*"
  to = "/public/:splat"
  status = 200
  condition = "File not found"